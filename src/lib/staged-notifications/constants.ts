/* =============
Staged-notification pipeline tuning
============= */

/** Pre-compute look-ahead window (seconds). Should stay aligned with default pass delay. */
export const PRECOMPUTE_WINDOW_SECONDS = 30;
